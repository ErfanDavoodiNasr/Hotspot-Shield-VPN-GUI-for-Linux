v1.0.0
- Full architectural rewrite into a packaged Python application
- Explicit VPN state machine with tested transitions
- Hardened subprocess runner (no shell, timeouts, cleanup)
- Searchable location selector with keyboard navigation
- One-click location switching while connected
- Secure credential storage (keyring + restricted fallback; .secrets for live tests)
- Quick installer / uninstaller with desktop integration
- Hermetic fake CLI, unit/integration/GUI/security/installer tests
- Docker + GitHub Actions CI (no live credentials in CI)
- Rewritten non-technical README
